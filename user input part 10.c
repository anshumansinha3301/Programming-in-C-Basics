// square of number
#include<stdio.h>
int main(){
    int x;
    printf("enter a number: ");
    scanf("%d", &x);
    printf("square of number is: %d", x*x); // here we multiplied the number to itself to get the square
    return 0;}
